<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Default Locales
    |--------------------------------------------------------------------------
    |
    | These are the locales that will be used by default throughout your app.
    |
    */
    'locales' => [
        'en' => 'English',
        'ar' => 'العربية',
    ],

    /*
    |--------------------------------------------------------------------------
    | Fallback Locale
    |--------------------------------------------------------------------------
    |
    | This is the fallback locale that will be used when a translation
    | is not available in the requested locale.
    |
    */
    'fallback_locale' => 'en',

    /*
    |--------------------------------------------------------------------------
    | Use Fallback for Empty Values
    |--------------------------------------------------------------------------
    |
    | When this is set to true, empty translations will fall back to the
    | fallback locale. When set to false, empty strings will be returned.
    |
    */
    'use_fallback_for_empty' => true,
];
